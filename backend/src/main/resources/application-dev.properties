# Dev profile. Inherits everything from application.properties and only adds the
# dev-only seed location, so sample departments/users are loaded under `dev` but
# never in prod (no profile) or test (application-test.properties).
spring.flyway.locations=classpath:db/migration,classpath:db/seed
